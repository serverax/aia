# Handover Notes (2026-05-25)

## What Was Handed Over

- Main branch includes merged PR #6 hardening and CI stabilization fixes.
- CI pipeline is currently green for:
  - Lint
  - Unit tests
  - Integration tests
  - Quality checks

## Operational Runbook

1. Sync local branch:
   - `git checkout main`
   - `git pull --ff-only`
2. Lint:
   - `python -m flake8 libs services tests --max-line-length=100 --extend-ignore=E203,W503`
3. Unit tests:
   - `python -m pytest tests/unit services/echo_agent/tests services/orchestrator_agent/tests services/compliance_agent/tests services/analyst_agent/tests services/tool_sandbox/tests -m unit -q`
4. PR checks:
   - `gh pr checks <PR_NUMBER>`

## Important Configuration Notes

- Root `requirements.txt` now includes:
  - `qdrant-client==1.18.0`
  - `sentence-transformers==3.1.1`
- Pytest portability settings (in `pyproject.toml`):
  - `--basetemp=.pytest_tmp`
  - `cache_dir=.pytest_cache`
- Transitional lint handling for legacy test files is centralized in `.flake8`.

## Remaining Follow-Ups (Owner: Platform/Infra)

- Validate security admission/network policy tests on live cluster environment.
- Confirm Talos credential handoff and infrastructure realignment tasks.
- Decide if OpenTelemetry shutdown warning should be hardened in local test harnesses.

## Support Guidance

- For CI failures, first inspect:
  - dependency resolution (`pip install -r requirements-dev.txt`)
  - pytest temp/cache settings
  - any new lint violations outside scoped legacy exceptions
- For regressions, open a focused fix branch from `main` and keep changes narrowly scoped to the failing gate.
