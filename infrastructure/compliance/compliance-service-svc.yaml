apiVersion: v1
kind: Service
metadata:
  name: compliance-service
  namespace: ordinox-ai
  labels:
    app: compliance-service
spec:
  type: ClusterIP
  selector:
    app: compliance-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
