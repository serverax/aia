apiVersion: v1
kind: Pod
metadata:
  name: analyst-test
  namespace: ordinox-ai
spec:
  containers:
  - name: analyst-test
    image: python:3.11-slim
    command: ["sleep", "3600"]
    resources:
      requests:
        memory: "2Gi"
        cpu: "500m"
      limits:
        memory: "4Gi"
        cpu: "1000m"
