fastapi==0.104.1
uvicorn==0.24.0
# qdrant-client 2.7.0 does not exist (max is 1.x) — pin to a real version that
# matches the deployed Qdrant (chart ships v1.12.x).
qdrant-client==1.12.2
# 2.2.2 imports huggingface_hub.cached_download (removed in recent hub) -> crash.
sentence-transformers==3.3.1
# 0.7.7 has no .messages API (the code uses messages.create) and pins pydantic
# v1, conflicting with pydantic 2.5.0. Modern client fixes both.
anthropic==0.39.0
httpx==0.25.2
pydantic==2.5.0
pytest==7.4.3
pytest-asyncio==0.23.1
